# add-user-account-page

User account page: profile view + change password form. Adds GET /account and POST /account/password with current-password verification, password strength rules, and graceful error handling.
